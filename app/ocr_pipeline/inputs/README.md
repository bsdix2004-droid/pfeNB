# Production Input Utilities

This folder contains utilities for production input processing (e.g., `pdf_extractor.py`).

**Note:** Sample/test documents (PDFs, images) have been moved to `tests/fixtures/sample_inputs/` to keep the production code clean.
